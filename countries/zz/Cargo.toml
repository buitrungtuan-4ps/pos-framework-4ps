[package]
name        = "pos-country-zz"
version     = "0.1.0"
description = "Reference country module. Copy this directory to start a real country."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
repository.workspace   = true

[dependencies]
pos-country = { path = "../../crates/pos-country" }
pos-ports   = { path = "../../crates/pos-ports" }
pos-proto   = { path = "../../crates/pos-proto" }

[dev-dependencies]
pos-contract-tests = { path = "../../crates/pos-contract-tests" }
pos-fakes          = { path = "../../crates/pos-fakes" }

[lints]
workspace = true
